# No PMIC: poweroff parks the SoC with the rails up, so suspend is the cheaper state.
# Long press is reachable over BLE only — IR stops repeating before systemd's 5 s threshold.
[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff
